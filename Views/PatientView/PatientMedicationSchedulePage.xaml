<Page x:Class="PersonalizedHealthcareTrackingSystemFinal.Views.PatientView.PatientMedicationSchedulePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PersonalizedHealthcareTrackingSystemFinal.Views.PatientView"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PatientMedicationSchedulePage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource PageHeaderBorderStyle}" Padding="32,24">
            <StackPanel>
                <TextBlock Text="Virtual Medication Assistant" FontSize="28" FontWeight="Bold" Foreground="{StaticResource DarkText}"/>
                <TextBlock Text="Automated schedule based on your latest prescription (#RX-8821)." Foreground="{StaticResource TextSecondary}" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" Padding="32">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="350"/>
                    <ColumnDefinition Width="32"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">

                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="Today's Progress" FontWeight="Bold" FontSize="18" HorizontalAlignment="Center" Margin="0,0,0,24"/>

                            <Grid Width="140" Height="140" Margin="0,0,0,20">
                                <Border CornerRadius="70" BorderThickness="10">
                                    <Border.BorderBrush>
                                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                            <GradientStop Color="#e2e8f0" Offset="0.7"/>
                                        </LinearGradientBrush>
                                    </Border.BorderBrush>
                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <TextBlock Text="4/6" FontSize="32" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}" HorizontalAlignment="Center"/>
                                        <TextBlock Text="Doses Taken" FontSize="12" Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <TextBlock Text="Great job! Keep it up! 👍" FontWeight="SemiBold" Foreground="{StaticResource SuccessColor}" HorizontalAlignment="Center"/>
                            <TextBlock Text="Next dose at: 18:00" Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <DockPanel Margin="0,0,0,16">
                                <TextBlock Text="October 2023" FontWeight="SemiBold"/>
                                <TextBlock Text="Today" DockPanel.Dock="Right" Foreground="{StaticResource PrimaryColor}" FontWeight="Bold" Cursor="Hand"/>
                            </DockPanel>

                            <UniformGrid Columns="7" Rows="1" Margin="0,0,0,8">
                                <TextBlock Text="M" Style="{StaticResource ColumnTextBlockStyle}" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                                <TextBlock Text="T" Style="{StaticResource ColumnTextBlockStyle}" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                                <TextBlock Text="W" Style="{StaticResource ColumnTextBlockStyle}" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                                <TextBlock Text="T" Style="{StaticResource ColumnTextBlockStyle}" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                                <TextBlock Text="F" Style="{StaticResource ColumnTextBlockStyle}" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                                <TextBlock Text="S" Style="{StaticResource ColumnTextBlockStyle}" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                                <TextBlock Text="S" Style="{StaticResource ColumnTextBlockStyle}" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                            </UniformGrid>

                            <UniformGrid Columns="7" Rows="1">
                                <Border Width="30" Height="30" CornerRadius="15" Margin="2">
                                    <TextBlock Text="23" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Width="30" Height="30" CornerRadius="15" Margin="2">
                                    <TextBlock Text="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Width="30" Height="30" CornerRadius="15" Margin="2" Background="{StaticResource DangerBg}">
                                    <TextBlock Text="25" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource DangerColor}" FontWeight="Bold"/>
                                </Border>
                                <Border Width="30" Height="30" CornerRadius="15" Margin="2">
                                    <TextBlock Text="26" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Width="30" Height="30" CornerRadius="15" Margin="2" Background="{StaticResource PrimaryColor}">
                                    <TextBlock Text="27" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" FontWeight="Bold"/>
                                </Border>
                                <Border Width="30" Height="30" CornerRadius="15" Margin="2">
                                    <TextBlock Text="28" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#cbd5e1"/>
                                </Border>
                                <Border Width="30" Height="30" CornerRadius="15" Margin="2">
                                    <TextBlock Text="29" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#cbd5e1"/>
                                </Border>
                            </UniformGrid>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <Grid Grid.Column="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Width="2" HorizontalAlignment="Center" Background="{StaticResource LightBorder}" Margin="0,20,0,20"/>

                    <StackPanel Grid.ColumnSpan="2">

                        <StackPanel Margin="0,0,0,32">
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="40" Height="40" CornerRadius="20" Background="White" BorderBrush="{StaticResource SuccessColor}" BorderThickness="2" HorizontalAlignment="Center">
                                    <TextBlock Text="🌅" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Morning" FontSize="18" FontWeight="Bold"/>
                                    <TextBlock Text="Around 07:00 - 08:00" Foreground="{StaticResource TextSecondary}"/>
                                </StackPanel>
                            </Grid>

                            <Border Margin="60,0,0,12" Style="{StaticResource CardStyle}" Padding="16" Background="#f8fafc" Opacity="0.8">
                                <DockPanel>
                                    <Button DockPanel.Dock="Right" Style="{StaticResource BtnPrimary}" Background="{StaticResource SuccessColor}" Content="✓ Taken at 07:30" FontSize="12" Padding="12,6" IsHitTestVisible="False"/>
                                    <StackPanel Orientation="Horizontal">
                                        <Border Width="48" Height="48" CornerRadius="12" Background="{StaticResource StockOkBg}" Margin="0,0,16,0">
                                            <TextBlock Text="💊" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
                                        </Border>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="Amlodipine 5mg" FontWeight="Bold" FontSize="16"/>
                                            <Border Background="{StaticResource BgLight}" CornerRadius="10" Padding="8,2" HorizontalAlignment="Left" Margin="0,4,0,0">
                                                <TextBlock Text="1 Tab • After breakfast" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                                            </Border>
                                        </StackPanel>
                                    </StackPanel>
                                </DockPanel>
                            </Border>

                            <Border Margin="60,0,0,0" Style="{StaticResource CardStyle}" Padding="16" Background="#f8fafc" Opacity="0.8">
                                <DockPanel>
                                    <Button DockPanel.Dock="Right" Style="{StaticResource BtnPrimary}" Background="{StaticResource SuccessColor}" Content="✓ Taken at 07:35" FontSize="12" Padding="12,6" IsHitTestVisible="False"/>
                                    <StackPanel Orientation="Horizontal">
                                        <Border Width="48" Height="48" CornerRadius="12" Background="{StaticResource StockOkBg}" Margin="0,0,16,0">
                                            <TextBlock Text="💊" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
                                        </Border>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="Multivitamin" FontWeight="Bold" FontSize="16"/>
                                            <Border Background="{StaticResource BgLight}" CornerRadius="10" Padding="8,2" HorizontalAlignment="Left" Margin="0,4,0,0">
                                                <TextBlock Text="1 Tab • With meal" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                                            </Border>
                                        </StackPanel>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,32">
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="40" Height="40" CornerRadius="20" Background="White" BorderBrush="{StaticResource StatusPendingBackground}" BorderThickness="2" HorizontalAlignment="Center">
                                    <TextBlock Text="☀️" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Noon" FontSize="18" FontWeight="Bold"/>
                                        <TextBlock Text=" • Overdue" Foreground="{StaticResource StatusPendingBackground}" FontWeight="SemiBold" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                    </StackPanel>
                                    <TextBlock Text="Around 11:00 - 12:00" Foreground="{StaticResource TextSecondary}"/>
                                </StackPanel>
                            </Grid>

                            <Border Margin="60,0,0,0" Style="{StaticResource CardStyle}" Padding="16" Background="{StaticResource DangerBg}" BorderBrush="{StaticResource DangerColor}">
                                <DockPanel>
                                    <Button DockPanel.Dock="Right" Style="{StaticResource BtnPrimary}" Background="{StaticResource StatusPendingBackground}" Content="Take Now" FontSize="12" Padding="12,6"/>
                                    <StackPanel Orientation="Horizontal">
                                        <Border Width="48" Height="48" CornerRadius="12" Background="{StaticResource DangerBg}" Margin="0,0,16,0">
                                            <TextBlock Text="💊" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
                                        </Border>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="Metformin 500mg" FontWeight="Bold" FontSize="16"/>
                                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                <Border Background="White" CornerRadius="10" Padding="8,2" Margin="0,0,8,0">
                                                    <TextBlock Text="1 Tab" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                                                </Border>
                                                <Border Background="#fff7ed" BorderBrush="#fdba74" BorderThickness="1" CornerRadius="10" Padding="8,2">
                                                    <TextBlock Text="⚠️ Important: Take with food" FontSize="12" Foreground="#c2410c" FontWeight="SemiBold"/>
                                                </Border>
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                        </StackPanel>

                        <StackPanel>
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="40" Height="40" CornerRadius="20" Background="White" BorderBrush="{StaticResource PrimaryColor}" BorderThickness="2" HorizontalAlignment="Center">
                                    <TextBlock Text="🌙" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Evening (Upcoming)" FontSize="18" FontWeight="Bold"/>
                                    <TextBlock Text="Around 18:00 - 20:00" Foreground="{StaticResource TextSecondary}"/>
                                </StackPanel>
                            </Grid>

                            <Border Margin="60,0,0,0" Style="{StaticResource CardStyle}" Padding="16">
                                <DockPanel>
                                    <Button DockPanel.Dock="Right" Style="{StaticResource BtnPrimary}" Content="Mark as Taken" FontSize="12" Padding="12,6"/>
                                    <StackPanel Orientation="Horizontal">
                                        <Border Width="48" Height="48" CornerRadius="12" Background="#e0f2fe" Margin="0,0,16,0">
                                            <TextBlock Text="💊" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" Foreground="{StaticResource PrimaryColor}"/>
                                        </Border>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="Atorvastatin 20mg" FontWeight="Bold" FontSize="16"/>
                                            <Border Background="{StaticResource BgLight}" CornerRadius="10" Padding="8,2" HorizontalAlignment="Left" Margin="0,4,0,0">
                                                <TextBlock Text="1 Tab • Before bed" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                                            </Border>
                                        </StackPanel>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                        </StackPanel>

                    </StackPanel>
                </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
