<Window x:Class="PersonalizedHealthcareTrackingSystemFinal.Views.ForgetPasswordWindow"
        x:Name="RootWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PersonalizedHealthcareTrackingSystemFinal.Views"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        Icon="/Resources/Logo/logo.ico"
        BorderThickness="0"
        Height="500"
        Width="700"
        FontFamily="/PersonalizedHealthcareTrackingSystemFinal;component/Resources/Fonts/#Poppins">    
    
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBackground" Color="#14161F"/>
        <SolidColorBrush x:Key="InputBackground" Color="#4F535E"/>
        <SolidColorBrush x:Key="AccentColor" Color="#38E5BD"/>
        <SolidColorBrush x:Key="TextWhite" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextGray" Color="#A0A0A0"/>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource InputBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextWhite}"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="10" 
                                BorderThickness="0">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="{StaticResource InputBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextWhite}"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="10" 
                                BorderThickness="0">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="Foreground" Value="#14161F"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="22">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#11161F" CornerRadius="20" BorderThickness="1" BorderBrush="#333">
        <Grid>
            <StackPanel Orientation="Horizontal" 
                        VerticalAlignment="Top" 
                        HorizontalAlignment="Right" 
                        Margin="0,10,15,0">

                <Button Content="—" 
                        Style="{StaticResource WindowControlButtonStyle}"
                        Command="{Binding MinimizeAppCommand}"
                        CommandParameter="{Binding ElementName=RootWindow}"/>

                <Button Content="✕" 
                        Style="{StaticResource CloseButtonStyle}"
                        Command="{Binding CloseAppCommand}"
                        CommandParameter="{Binding ElementName=RootWindow}"/>
            </StackPanel>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="320">
                <Viewbox Width="60" Height="60" HorizontalAlignment="Center" Margin="0,0,0,20">
                    <Path Fill="{StaticResource AccentColor}" 
                            Data="M12,21.35L10.55,20.03C5.4,15.36 2,12.27 2,8.5C2,5.41 4.42,3 7.5,3C9.24,3 10.91,3.81 12,5.08C13.09,3.81 14.76,3 16.5,3C19.58,3 22,5.41 22,8.5C22,12.27 18.6,15.36 13.45,20.03L12,21.35Z" />
                </Viewbox>

                <TextBlock Text="RECOVER ACCOUNT" 
                            Foreground="{StaticResource TextWhite}" 
                            FontSize="20" FontWeight="Bold" 
                            HorizontalAlignment="Center" Margin="0,0,0,5"/>

                <TextBlock Text="Enter your username to search our database." 
                           Foreground="{StaticResource TextGray}" 
                           FontSize="12" FontStyle="Italic"
                           HorizontalAlignment="Center" Margin="0,0,0,30"/>

                <TextBox Text="{Binding Input, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource RoundedTextBoxStyle}" 
                         Tag="Username or Email"
                         Height="50" Margin="0,0,0,20" Padding="15,12,20,12" FontFamily="Poppins"/>
 
                <Button x:Name="btnVerify" Content="SEARCH USER"
                        Command="{Binding SearchUserButtonCommand}"/>

                <TextBlock x:Name="lblMessage" 
                            Text="" Foreground="#FF6B6B" 
                            HorizontalAlignment="Center" Margin="0,10,0,0" 
                            TextWrapping="Wrap" TextAlignment="Center"/>

                <StackPanel x:Name="pnlResetSection" Visibility="Collapsed" Margin="0,20,0,0">
                    <Border Background="#252836" CornerRadius="10" Padding="15">
                        <StackPanel>
                            <TextBlock Text="User Found!" Foreground="{StaticResource AccentColor}" 
                                        HorizontalAlignment="Center" FontWeight="Bold" Margin="0,0,0,10"/>

                            <Grid Margin="0,0,0,15">
                                <PasswordBox x:Name="txtNewPassword" Height="50"/>
                            </Grid>

                            <Button Content="RESET PASSWORD"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <Button Background="Transparent"
                        HorizontalAlignment="Center"
                        Margin="0,30,0,0"
                        BorderBrush="Transparent"
                        Cursor="Hand"
                        Command="{Binding BackToLogInButtonCommand}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="Transparent">
                                <ContentPresenter/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <TextBlock Text="Back to Login" Foreground="{StaticResource AccentColor}" 
                                Cursor="Hand" TextDecorations="Underline"/>
                </Button>
                    
            </StackPanel>
        </Grid>
    </Border>
</Window>
