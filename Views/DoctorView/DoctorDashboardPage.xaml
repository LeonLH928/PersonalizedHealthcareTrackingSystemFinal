<Page x:Class="PersonalizedHealthcareTrackingSystemFinal.Views.DoctorView.DoctorDashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PersonalizedHealthcareTrackingSystemFinal.Views.DoctorView"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DoctorDashboardPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Border Style="{StaticResource PageHeaderBorderStyle}" 
                Padding="32,16"> 
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel
                    HorizontalAlignment="Left"
                    Orientation="Vertical">

                    <TextBlock
                        Style="{StaticResource PageHeaderTextBlockStyle}"
                        Text="Doctor's Dashboard"/>

                    <Border 
                        Style="{StaticResource DateBadgeBorderStyle}"
                        Margin="0,8,0,0">
                        <TextBlock
                            Style="{StaticResource SecondaryTextTextBlockStyle}"
                            Text="Wed, Oct 26, 2025"/>
                    </Border>

                </StackPanel>

                <StackPanel
                    Grid.Column="1"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel
                            Orientation="Vertical"
                            VerticalAlignment="Center">
                            <TextBlock
                                FontWeight="SemiBold"
                                FontSize="18"
                                FontFamily="Arial"
                                TextAlignment="Right"
                                Text="Dr. Tran Minh Tuan"/>

                            <TextBlock
                                Margin="0,4,0,0"
                                Text="Cardiologist"
                                Style="{StaticResource SecondaryTextTextBlockStyle}"
                                TextAlignment="Right"/>
                        </StackPanel>

                        <Border
                            Grid.Column="1"
                            Height="60"
                            Width="60"
                            Margin="10,0,0,0"
                            CornerRadius="30"
                            Background="#cbd5e1"/>
                    </Grid>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
