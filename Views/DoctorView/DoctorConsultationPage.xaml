<Page x:Class="PersonalizedHealthcareTrackingSystemFinal.Views.DoctorView.DoctorConsultationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PersonalizedHealthcareTrackingSystemFinal.Views.DoctorView"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DoctorConsultationPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Style="{StaticResource PageHeaderBorderStyle}" 
                Padding="32,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">

                    <TextBlock
                        Style="{StaticResource PageHeaderTextBlockStyle}"
                        Text="Nguyen Van A"
                        Margin="0,0,10,0"/>

                    <TextBlock
                        Style="{StaticResource SecondaryTextTextBlockStyle}"
                        VerticalAlignment="Center"
                        FontSize="18"
                        Text="(ID: P-123456)"/>

                </StackPanel>

                <StackPanel
                    Grid.Column="1"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel
                            Orientation="Vertical"
                            VerticalAlignment="Center">
                            <TextBlock
                                FontWeight="SemiBold"
                                FontSize="18"
                                FontFamily="Segoe UI"
                                TextAlignment="Right"
                                Text="Dr. Tran Minh Tuan"/>

                            <TextBlock
                                Margin="0,4,0,0"
                                Text="Cardiologist"
                                Style="{StaticResource SecondaryTextTextBlockStyle}"
                                TextAlignment="Right"/>
                        </StackPanel>

                        <Border
                            Grid.Column="1"
                            Style="{StaticResource DefaultDoctorAvatar}"/>
                    </Grid>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
