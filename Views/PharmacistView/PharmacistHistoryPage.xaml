<Page x:Class="PersonalizedHealthcareTrackingSystemFinal.Views.PharmacistView.PharmacistHistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PersonalizedHealthcareTrackingSystemFinal.Views.PharmacistView" xmlns:fa="http://schemas.fontawesome.io/icons/"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PharmacistHistoryPage">

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource PageHeaderBorderStyle}" Padding="24,20" Margin="0,0,0,24" CornerRadius="8">
            <StackPanel>
                <TextBlock Text="Completed Prescription History" 
                           FontSize="24" FontWeight="Bold" Foreground="{StaticResource DarkText}"/>
                <TextBlock Text="View and track successfully dispensed prescriptions." 
                           FontSize="14" Foreground="{StaticResource TextSecondary}" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="16" Margin="0,0,0,24">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border BorderBrush="{StaticResource LightBorder}" BorderThickness="1" CornerRadius="8" Padding="8,0" HorizontalAlignment="Left" Width="350">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <fa:FontAwesome Icon="Search" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="8,0"/>
                        <TextBox Grid.Column="1" BorderThickness="0" Background="Transparent" 
                                 VerticalContentAlignment="Center" Padding="4"
                                 Text="Search by Rx ID, Patient or Pharmacist..."/>
                    </Grid>
                </Border>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="16">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="From:" VerticalAlignment="Center" Foreground="{StaticResource TextSecondary}" Margin="0,0,8,0"/>
                        <DatePicker Style="{StaticResource StandardDatePicker}" SelectedDate="2023-10-01" Width="130"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="To:" VerticalAlignment="Center" Foreground="{StaticResource TextSecondary}" Margin="0,0,8,0"/>
                        <DatePicker Style="{StaticResource StandardDatePicker}" SelectedDate="2023-10-27" Width="130"/>
                    </StackPanel>

                    <Button Style="{StaticResource BtnSecondary}" Padding="16,8" Margin="8,0,0,0">
                        <StackPanel Orientation="Horizontal">
                            <fa:FontAwesome Icon="FileExcelOutline" Margin="0,0,8,0"/>
                            <TextBlock Text="Export Excel"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2" Style="{StaticResource InventoryDataGrid}" RowHeight="70">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Rx ID" Binding="{Binding Id}" Width="100" FontWeight="Bold" Foreground="{StaticResource SidebarActive}"/>

                <DataGridTemplateColumn Header="Completion Time" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{Binding Date}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Time}" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Patient" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{Binding PatientName}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding PatientId}" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Prescriber" Binding="{Binding DoctorName}" Width="180"/>

                <DataGridTextColumn Header="Medication Summary" Binding="{Binding MedSummary}" Width="*" Foreground="{StaticResource TextSecondary}"/>

                <DataGridTextColumn Header="Dispensed By" Binding="{Binding PharmacistName}" Width="150"/>

                <DataGridTemplateColumn Header="Status" Width="140">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource StockBadgeOk}" Background="#dcfce7" HorizontalAlignment="Left">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <TextBlock Text="✓" Foreground="#15803d" FontWeight="Bold" Margin="0,0,4,0"/>
                                    <TextBlock Text="Dispensed" Foreground="#15803d" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Actions" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="8">
                                <Button Style="{StaticResource BtnSecondary}" Content="👁️ Details" Padding="10,6" FontSize="12"/>
                                <Button Style="{StaticResource BtnSecondary}" Content="🖨️ Reprint" Padding="10,6" FontSize="12"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="3" Margin="0,16,0,0">
            <TextBlock Text="Showing 1-10 of 1,250 records" VerticalAlignment="Center" Foreground="{StaticResource TextSecondary}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="← Previous" Style="{StaticResource BtnSecondary}" Padding="12,6" Margin="0,0,4,0" IsEnabled="False"/>
                <Button Content="1" Style="{StaticResource PrimaryButton}" Padding="12,6" Margin="0,0,4,0"/>
                <Button Content="2" Style="{StaticResource BtnSecondary}" Padding="12,6" Margin="0,0,4,0"/>
                <Button Content="3" Style="{StaticResource BtnSecondary}" Padding="12,6" Margin="0,0,4,0"/>
                <Button Content="Next →" Style="{StaticResource BtnSecondary}" Padding="12,6"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
