<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Page Header Border Style -->
    <Style x:Key="PageHeaderBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource LightBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- Sidebar Border Style -->
    <Style x:Key="SidebarBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="#141C24"/>
        <Setter Property="Width" Value="55"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation 
                                Storyboard.TargetProperty="(Border.Width)"
                                To="220" Duration="0:0:0.3"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation 
                                Storyboard.TargetProperty="(Border.Width)"
                                To="55" Duration="0:0:0.3"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </Style.Triggers>
    </Style>
    
    <!-- Secondary Text Border Style -->
    <Style x:Key="SecondaryTextBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource TextSecondaryBorder}"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="BorderBrush" Value="#e2e8f0"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>
    
    <!-- Default User Avatar -->
    <Style x:Key="DefaultDoctorAvatar" TargetType="Border">
        <Setter Property="Height" Value="60"/>
        <Setter Property="Width" Value="60"/>
        <Setter Property="Margin" Value="10,0,0,0"/>
        <Setter Property="CornerRadius" Value="30"/>
        <Setter Property="Background" Value="#cbd5e1"/>
    </Style>
    
    <!-- Page content Border Style -->
    <Style x:Key="PageContentBorderStyle" TargetType="Border">
        <Setter Property="Padding" Value="32"/>
        <Setter Property="Background" Value="#F1F5F9"/>
    </Style>

    <!-- Base Custom Border Style -->
    <Style x:Key="BaseCustomBorderStyle" TargetType="Border">
        <Setter Property="Padding" Value="5"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="CornerRadius" Value="8"/>
    </Style>
    
    <!-- Widget Border Style -->
    <Style x:Key="WidgetBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="White"/>
        <Setter Property="Padding" Value="20"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="BorderBrush" Value="{StaticResource LightBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- Next patient Border Style -->
    <Style x:Key="NextPatientBorderStyle" TargetType="Border">
        <Setter Property="Padding" Value="24"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="BorderBrush" Value="#4A90E2"/>
        <Setter Property="BorderThickness" Value="8,0,0,0"/>
        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#EFF6FF"/>
                    <GradientStop Color="White" Offset="0.5"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Happening patient Border Style -->
    <Style x:Key="HappeningPatientBorderStyle" TargetType="Border">
        <Setter Property="Padding" Value="24"/>
        <Setter Property="BorderBrush" Value="#26B4E2"/>
        <Setter Property="BorderThickness" Value="8,0,0,0"/>
        <Setter Property="Background" Value="#ACC7E0"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="Black"
                                  Direction="100"
                                  ShadowDepth="0"
                                  BlurRadius="0"
                                  Opacity="0.3"/>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation 
                                Storyboard.TargetProperty="(Border.Effect).(DropShadowEffect.BlurRadius)"
                                To="20" Duration="0:0:0.2"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation 
                                Storyboard.TargetProperty="(Border.Effect).(DropShadowEffect.BlurRadius)"
                                To="0" Duration="0:0:0.2"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </Style.Triggers>
    </Style>


    <!-- Upcoming patient Border Style -->
    <Style x:Key="UpcomingPatientBorderStyle" TargetType="Border">
        <Setter Property="Padding" Value="24"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
        <Setter Property="BorderThickness" Value="8,0,0,0"/>
        <Setter Property="Background" Value="{StaticResource LightWaitingStatusBorder}"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="Black"
                                  Direction="100"
                                  ShadowDepth="0"
                                  BlurRadius="0"
                                  Opacity="0.3"/>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation 
                                Storyboard.TargetProperty="(Border.Effect).(DropShadowEffect.BlurRadius)"
                                To="20" Duration="0:0:0.2"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation 
                                Storyboard.TargetProperty="(Border.Effect).(DropShadowEffect.BlurRadius)"
                                To="0" Duration="0:0:0.2"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Default Dashboard Avatar -->
    <Style x:Key="DefaultDashboardAvatar" TargetType="Border">
        <Setter Property="Height" Value="80"/>
        <Setter Property="Width" Value="80"/>
        <Setter Property="Margin" Value="10,0,0,0"/>
        <Setter Property="CornerRadius" Value="40"/>
        <Setter Property="Background" Value="#e0f2fe"/>
    
    </Style>
    
    <!-- Line -->
    <Style x:Key="Line" TargetType="Border">
        <Setter Property="BorderBrush" Value="{StaticResource LightBorder}"/>
        <Setter Property="BorderThickness" Value="0.5"/>
    </Style>

    <!-- Dot -->
    <Style x:Key="Dot" TargetType="Border">
        <Setter Property="Height" Value="16"/>
        <Setter Property="Width" Value="16"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Margin" Value="0,0,8,0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
    </Style>
    
    <!-- Urgent Border Style -->
    <Style x:Key="UrgentBorderStyle" TargetType="Border" BasedOn="{StaticResource BaseCustomBorderStyle}">
        <Setter Property="Background" Value="{StaticResource UrgentTagBorder}"/>
        <Setter Property="Background" Value="{StaticResource UrgentTagBorder}"/>
        <Setter Property="BorderBrush" Value="#fecaca"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>
</ResourceDictionary>